# Required for Strict Mode sports edges (free tier): https://the-odds-api.com
# Without this key, sports markets are blocked for lacking external fair value.
# cp .env.example .env  →  paste key  →  restart npm run dev
VITE_ODDS_API_KEY=
