# Optional for npm run dev (public market data via Vite proxy — no keys).
#
# For near-real paper MM with signed read-only orderbooks (npm run dev:real):
# Prefer process env, or box-secrets.json on the agent box.
# NEVER commit real keys. Browser never sees the private key.
#
# KALSHI_KEY_ID=
# KALSHI_PRIVATE_KEY=
# MM_PROXY_PORT=8787
