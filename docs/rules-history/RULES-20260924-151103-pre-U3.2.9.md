# Paper 15m MM playbook (RULES)

**PAPER ONLY.** Read-only research sim — never places live Kalshi orders. Green paper P&L ≠ live edge.

## U3.2 — House rules v1 (primary)

**Mid-centered maker** — quote around **book mid**, not digital FV. Not Family E FV opens, not S1–S5, not complete-set yet. User rejected FV–mid mismatch / gap trading as the target.

- **Center:** reservation `r = mid − skew(q,τ)`; posts `r ± halfSpread` with inventory skew (same skew helpers as before). Maker-only BBO join when L2 present.
- **FV:** may still be computed for UI/telemetry — **must not** center `yesBid`/`yesAsk` or rank/select by `|FV−mid|`.
- **Hard τ:**
  - **Blackout (flat):** `minutesRemaining ≤ blackoutMinutes` (default **0.75**) and `q === 0` → both OFF, `U3.1: settlement blackout`.
  - **Blackout flatten (U3.1.2):** same τ window but `q ≠ 0` → flatten-only (long → ask; short → bid), tag `blackout_flatten`, strip `U3.1.2: blackout flatten — exit only`.
  - **No new opens when flat (U3.2.4):** `minutesRemaining ≤ noOpenMinutes` (default **4**) and `q === 0` → both OFF, `U3.2.4: no new opens — τ≤4m`.
  - **Soft reduce (U3.2.4):** `hardFlatMinutes < τ ≤ noOpenMinutes` and `q ≠ 0` → reduce-only (maker OK; no new risk on the other side).
  - **HardFlat flatten (U3.2.1):** `minutesRemaining ≤ hardFlatMinutes` (default **2**) and `q ≠ 0` → only reducing side ON; prices through the touch (hit bid / lift ask).
- **Hard Q:** `|q| ≥ maxInventory` → withdraw the adding side.
- **Fills:** U2.13 L2 queue fills only when live book; no soft fills when L2 off.
- **Invalid mid:** park fail-loud `U3.2: no mid — needs two-sided book`.
- **Tags:** `house_mid` / `house_cover` / `house_close` / `house_soft_exit` / `flatten` / `blackout` / `blackout_flatten` / `extreme_mid` — **not** S1–S5.
- **Strip:** `U3.2: house mid quotes` when mid-centered opens are armed.
- **Ranking (U3.2):** prefer L2 / mid quality / tighter spread — **not** larger `|FV−mid|` (`U3.2: rank by L2 / mid quality — not |FV−mid|`).

Config: `quotingEnabled: true` (default for new sessions / v1 Start). Headless may stay stopped with `quotingEnabled: false` — when re-enabled, house mid applies. Set `false` to restore U3.0 pause strip.

## U3.1.1 — refuse opens at extreme mid (retained)

Pinned books near **0¢ / 100¢** must not open new risk.

- **No new longs** (`buy_yes`) when `mid ≥ toxicMidHigh` (default 0.95).
- **No new shorts** (`sell_yes`) when `mid ≤ toxicMidLow` (default 0.05).
- Same gate on quote arms (`decideQuoteSides`) and fill accept (`isToxicExtremeMid` / `applyFill`).
- **Flatten/reduce** still allowed when inventory ≠ 0 and the side reduces.
- Flat + extreme mid → park both with `U3.1.1: extreme mid — no new opens`.

## U3.1.2 — blackout flatten when inventory (retained)

Settlement blackout must not trap open inventory. In the last `blackoutMinutes`:

- **Flat** → park both (unchanged).
- **Long / short** → flatten-only via side arms; U3.1.1 extreme-mid still refuses *opens*; reduce side stays allowed.
- Settled / `mins ≤ 0` settlement path unchanged.

## U3.2.1 — last-τ flatten must actually exit (not silent maker park)

In `hardFlatMinutes` / `blackoutMinutes` with `q ≠ 0`, flatten is not optional:

- **Long** → sell YES by **hitting the best bid** (taker-through-touch). **Short** → buy YES by **lifting the best ask**.
- Maker-only join is wrong here: a resting ask above a 1¢ dump never gets lifted while the book bleeds out.
- Engine allows `taker_cross` **only** for flatten / blackout_flatten reducing fills (strict realism still blocks taker opens).
- If exit liquidity is missing (long + no bid, short + no ask) → fail-loud `U3.2.1: stuck inventory, no bid/ask` on that book — never silent hold.
- Do **not** revive S1–S5 or FV-centered opens.

## Slot ops (not quote scenarios)

- **SLOT_EVICT** — legacy sanity+|FV−mid| eviction is **off** under U3.2 (FV gap is telemetry only).
- **L2_OFF_EVICT (U2.14 / U2.14.1)** — `useLiveBook && !liveBook` for `l2OffDropTicks` consecutive syncs + flat → drop & refill; open inventory holds with `U2.14: L2 off — holding inv until flat`.

## Scarcity / caps (unchanged intent)

- Per-ticker + portfolio rolling fill caps.
- Maker-only under strict; `fillMidFallback: false` under strict.

## Journal / digests

Optional `scenarioId` on fills may be plain house tags or historical S* digests — **never** drives quotes via S1–S5 gates.
**New writes** stamp `house_mid` / `house_cover` / `house_close` / flatten tags — never emit S1–S5. Digests still *read* old S3 rows.

## Records forever (archive)

- Rules snapshots: `docs/rules-history/` (append-only; each U3.x.y copies RULES before edit).
- Dig / session tapes: `data/paper-mm/archive/` (+ `INDEX.md`). Runtime `data/paper-mm/*` journals are **never auto-purged**.
- Do not trash rules or tapes until the user says so.

## Retired (not primary)

- **Family E** digital-FV-centered opens (U3.1) — superseded by U3.2 house mid.
- FV–mid mismatch / gap trading — explicitly rejected as product target.
- Complete-set complementary bids — not in this slice.

## U3.2.2 — UI disk journal (durable tape)

Browser paper fills must also append to `data/paper-mm/ui-journal.jsonl` via mm-proxy `POST /local-api/paper-mm/journal` (plus `ui-run-meta.json` on Start). localStorage remains; disk journal is the recoverable tape. Fail-loud if journal POSTs fail repeatedly. Do not reintroduce S1–S5.

## U3.2.3 — no late opens + curb low-mid longs + house_cover tags

Dig (Mac UI ~4:58–6:01 CT, 50 fills, +$5.67): wins were short YES → cover near expiry; **all 7 losses** were `house_mid` long opens that later `flatten` sold @1¢; one BTC open with ~1.6m left then flatten 6s later.

1. **No new opens when flat near expiry** — originally τ ≤ hardFlatMinutes (2); **extended in U3.2.4** to `noOpenMinutes` (4). `q ≠ 0` still flattens (U3.2.1).
2. **Long-open curb:** refuse **NEW** long YES (`buy_yes` opens) when `mid ≤ longOpenMinMid` (default raised to **0.50** in U3.2.5; was 0.40). Strip `U3.2.5: long open curb — mid ≤50¢`. **Do not ban shorts** — that’s the money. Covering a short (bid while `q < 0`) still allowed.
3. **Tags:** profitable cover/close stamps `house_cover` (was leftover `S3`); risk/stuck closes → `house_close`. No new S1–S5 on fill path.
4. **Archive forever:** `docs/rules-history/` + `data/paper-mm/archive/` — never auto-delete.

Config: `longOpenMinMid: 0.50` (fail-loud / migrate if missing). Paper-only.

## U3.2.4 — park flat opens for ≤4m (soft reduce 2–4m)

Extend the late-open park so flat books do not re-enter in the last **4** minutes (was 2 via hardFlat alone).

1. **Config:** `noOpenMinutes` default **4**; `hardFlatMinutes` stays **2**.
2. **Flat + τ ≤ noOpenMinutes** → both OFF, strip `U3.2.4: no new opens — τ≤4m`.
3. **Inv ≠ 0 + hardFlat < τ ≤ noOpen** → reduce-only / flatten-style arms (maker reduce OK — do not force taker).
4. **Inv ≠ 0 + τ ≤ hardFlatMinutes** → existing U3.2.1 flatten-to-touch.
5. Keep U3.2.3/5 gates: `longOpenMinMid` (see U3.2.5), `house_cover` tags, disk journal, archive forever.

## U3.2.5 — raise longOpenMinMid 0.40 → 0.50

U3.2.4 midrun losses: ZEC long mid=0.49 (−$0.48) and BTC long mid=0.425 (−$0.40) — both **above** the 0.40 curb, **below** 0.50, then bled via flatten.

1. **Config:** `longOpenMinMid` default **0.50** (was 0.40).
2. **Flat + mid ≤ 0.50** → long bid OFF (`U3.2.5: long open curb — mid ≤50¢`); short ask may stay.
3. **Shorts, covers, flatten/reduce** unchanged.
4. Keep U3.2.4 `noOpenMinutes=4`, `hardFlatMinutes=2`, `house_cover` tags, disk journal, archive forever.

Paper-only.

## U3.2.6 — force 50¢ curb on UI + soft-exit longs when mid ≤50¢

U3.2.5 session (~8:14pm CT): 28 fills, realized −$1.26. Audit: UI Start uses loose (`strictRealism: false`); `migratePersistedScarcityConfig` early-returned when `!strict` so persisted **0.40** stayed live; `presetsForMode(false)` omitted `longOpenMinMid`. Longs opened above 50¢ (SOL 55–57¢, XRP 59¢) then rode to ~1–7¢ at hardFlat → big $ hole.

1. **Migrate always lifts curb:** loose AND strict bump `longOpenMinMid` to `max(saved, STRICT 0.50)` when missing or saved < 0.50 — never leave 0.40 stuck.
2. **`presetsForMode`:** include `longOpenMinMid` (loose/strict both carry 0.50 from STRICT/LOOSE base).
3. **UI Start:** `mmRunner` `setConfig` explicitly passes `longOpenMinMid: 0.5` every Start.
4. **Soft-exit longs:** when `inventory > 0` and `mid ≤ longOpenMinMid`, force hit-touch flatten ask (same path as hardFlat), tag **`house_soft_exit`**, even if `τ > hardFlatMinutes`. Covers/shorts still OK; flat + mid≤50¢ still curbs new long bids (U3.2.5).
5. **`isFlattenHouseTag`** includes `house_soft_exit` so L2 taker_cross / rate-cap bypass match hardFlat exits.

Paper-only. Reset + Start after deploy so config reloads.

## U3.2.7 — UI Start = strict L2; refuse absurd $0 fills

U3.2.6 3×15m dig (+$5.20 tape): curb + soft_exit worked, but **loose** Start (`strictRealism: false`, `allowMidWalk: true`, taker allowed) minted fake P&L — **4× buy_yes @ price=0** while yesBid≈0.97 / yesAsk≈0.99 / mid≈0.98–0.99 (taker_cross covers/flattens on NEAR/HYPE/SOL/DOGE, ~+$2.24). Do **not** treat +$5.20 as live edge.

1. **UI Start = STRICT L2:** `mmRunner` + legacy `PaperMmPanel` Start force `setStrictRealism(true)`, `presetsForMode(true)`, `strictRealism: true`, `allowMidWalk: false`, `fillMidFallback: false`, `useLiveBook: true`. Keep house mid knobs: `longOpenMinMid: 0.5`, `noOpenMinutes: 4`, `hardFlatMinutes: 2`.
2. **Under STRICT:** no mid_walk; taker_cross refused except existing flatten/soft_exit reduce path; no fillMidFallback soft universe path.
3. **Hard refuse absurd prices:** `applyFill` (non-settlement) refuses fill `price` not within ε of the relevant touch (buy ≈ ask taker / bid maker; sell ≈ bid taker / ask maker) when yesBid/yesAsk known. **Always** refuse **price ≈ 0** when mid or touch ≥ **0.50** (scam pattern). Strip `U3.2.7: ABSURD FILL REFUSED …`.
4. Keep mid-centered house rules; no S1–S5.

Paper-only. **Reset + Start** after deploy.

## U3.2.8 — usable strict L2 (depth 3 / touch 2) + maker join BBO

U3.2.7 killed soft fills (correct), but `minBookDepthConsumed: 8` + `minTouchPolls: 4` plus quotes resting **behind** touch starved real L2 activity — user saw near-dead paper MM for days.

1. **Scarcity knobs (still pure L2):** `minBookDepthConsumed` **8→3**, `minTouchPolls` **4→2**. Keep `strictRealism: true`, `allowMidWalk: false`, `fillMidFallback: false`. Absurd ~$0 refuse when mid/touch ≥50¢ unchanged.
2. **Maker join BBO:** `clampQuotesMakerOnly` lifts bid / drops ask **to touch** when desired mid±half sits behind (never improve/cross). Quotes spend time AT BBO so the L2 queue can burn.
3. **Start / migrate:** v1 `mmRunner` + legacy panel pin depth 3 / touch 2; strict migrate snaps older 8/4 down to 3/2 and forces soft paths off.
4. Keep house mid, inventory+τ flatten, extreme-mid refuse, `longOpenMinMid` 0.50, disk journal, append-only archive. No S1–S5 / no soft-sim.

Paper-only. **Reset + Start** after hard refresh.

